# Test Environment Variables
# These are used during test runs and should be safe/fake values

# Supabase (will be mocked in tests)
SUPABASE_URL=https://test.supabase.co
SUPABASE_ANON_KEY=test-anon-key
SUPABASE_SERVICE_KEY=test-service-key

# Clerk (will be mocked in tests)
CLERK_SECRET_KEY=test_clerk_secret_key
CLERK_PUBLISHABLE_KEY=pk_test_placeholder

# Anthropic (will be mocked in tests)
ANTHROPIC_API_KEY=sk-test-placeholder

# OpenAI (will be mocked in tests)
OPENAI_API_KEY=sk-test-placeholder

# Stripe (will be mocked in tests)
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_test_placeholder

# Redis (will be mocked in tests)
REDIS_URL=redis://localhost:6379/0

# Environment
ENVIRONMENT=test
DEBUG=false
LOG_LEVEL=WARNING
